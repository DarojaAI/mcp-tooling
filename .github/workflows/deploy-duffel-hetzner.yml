name: Deploy Duffel to Hetzner

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action (plan/apply/destroy)'
        required: true
        default: 'plan'

# This is a Phase 4 placeholder workflow to satisfy contract validation.
# Full implementation coming in Phase 4.

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Deploy workflow placeholder - Phase 4 implementation pending"
          echo "Contract vars: ${{ vars.SERVER_NAME }}, ${{ vars.HETZNER_LOCATION }}, ${{ vars.SERVER_TYPE }}"
          echo "Contract vars: ${{ vars.HETZNER_SSH_KEY_NAME }}, ${{ vars.HCX_STORAGE_URL }}"
          echo "Contract vars: ${{ vars.TERRAFORM_STATE_BUCKET }}, ${{ vars.TERRAFORM_STATE_KEY }}"
          echo "Contract secrets: ${{ secrets.HETZNER_API_TOKEN && 'present' || 'missing' }}"
          echo "Contract secrets: ${{ secrets.HCX_ACCESS_KEY && 'present' || 'missing' }}"
          echo "Contract secrets: ${{ secrets.HCX_SECRET_KEY && 'present' || 'missing' }}"
          echo "Contract secrets: ${{ secrets.SSH_PRIVATE_KEY && 'present' || 'missing' }}"
          echo "Contract secrets: ${{ secrets.DUFFEL_API_KEY && 'present' || 'missing' }}"
          echo "Contract secrets: ${{ secrets.MCPTOOLING_ALLOWED_TOKENS && 'present' || 'missing' }}"
